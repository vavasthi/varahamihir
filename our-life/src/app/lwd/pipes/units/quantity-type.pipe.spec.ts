import { QuantityTypePipe } from './quantity-type.pipe';

describe('QuantityTypePipe', () => {
  it('create an instance', () => {
    const pipe = new QuantityTypePipe();
    expect(pipe).toBeTruthy();
  });
});
