import { PageableIngredient } from './pageable-ingredient';

describe('PageableIngredient', () => {
  it('should create an instance', () => {
    expect(new PageableIngredient()).toBeTruthy();
  });
});
