import { CustomSidenavMenuItems } from './custom-sidenav-menu-items';

describe('CustomSidenavMenuItems', () => {
  it('should create an instance', () => {
    expect(new CustomSidenavMenuItems()).toBeTruthy();
  });
});
